# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
*.egg-info/

# Virtual environments
venv/
.venv/

# Runtime data / logs — personal/machine-specific, doesn't belong in the repo
seen-houses.json
*.log

# Private personal documents kept next to the code (spreadsheets and their
# dated backups) — never part of the public repo
*.xlsx
*.xls
*.ods
*.csv
~$*
.~lock.*#
backup/

# Secrets — must never end up in the repo
.env
secrets.env

# IDE
.idea/
.vscode/

# Claude Code local settings — machine-specific (local paths, LAN IPs from
# past debugging sessions), not meant to be shared. .claude/settings.json
# itself (the project-level hooks config) stays tracked on purpose.
.claude/settings.local.json
